#include main.h

/**
 * _isdigit - checks for digits 0 thru 9
 * @c: digit is to be checked
 *
 * Return 1 if c is a digit, else return 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (10);
	}
	else
	{
		return (0);
	}
}
